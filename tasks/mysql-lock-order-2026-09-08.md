# 并发授权第二轮：锁顺序与失败回滚

## 后续交付授权（2026-09-08）

- 有限收尾完成后，用户确认提交并推送当前分支、创建面向 `master` 的 PR，待 CI 全部通过后合并；不部署。下文“未提交/未推送”保留本地收尾阶段事实。
- 提交前再次复跑：Django Ruff 与全量 pytest（290 passed / 1 skipped）、FastAPI 完整 `make quality`（856 passed / 1 skipped，覆盖率 89.25%）通过；FastAPI 沿用仅测试进程的密码配置覆盖。
- 根 unittest 首次因沙箱内隔离 Nginx 未启动而失败，获准在沙箱外重跑 42 项全部通过，未修改业务或测试。远端 MySQL 56 项结果以本次 PR 的 CI 为准。

## 目标与边界

- 分支：`codex/mysql-grant-lock-order`，基于已合并 #376 的 `add845e`。
- 测试先行验证更新/删除交错、多管理员交叉修改和数据库故障后的事务原子性；只修复实证问题。
- 不新增自动重试、全局锁、API 或迁移，不改前端或真实配置；只连接专用临时 MySQL。
- 本轮不自动提交、推送、开 PR、合并或部署。
- 有限收尾按企业内部低并发定位，仅复核已有四个业务文件、保留回归测试并重跑既有门禁；不扩大 56 项 MySQL 矩阵。公共 API、响应格式、权限政策、数据库结构和依赖均保持不变。

## 执行

- [x] 两端真实复现角色单删、角色批删、用户批删与更新交错导致死锁；实际 `lock_deadlocks` 计数增加。
- [x] 删除及批量失败项重试入口统一先锁操作者，再锁目标；保留原锁内授权、角色保护和逐条事务。
- [x] 测试普通管理员的单删/批删/重试；两管理员交叉改用户时，一个提交、一个死锁失败，未自动重试。
- [x] 在真实标量和关联写入后，用独立连接及测试专用表构造 1213/1205；检查错误和数据回滚，不 mock 数据库异常。
- [x] 保留旧快照 24 项守卫，现有 CI 入口自动执行本轮新增检查。
- [x] 核验 GitHub #365–#376 的合并状态，更新过时任务记录；架构/技术债保留未消除的风险。
- [x] 最终门禁、diff 复核和专用容器清理。

## 此前验证记录

- RED：两端角色单删、角色批删和用户批删依次复现死锁；服务报 1213 或批量 `DELETE_FAILED`，死锁计数每次加一。按入口逐项修复后再继续下一场景，没有绕过失败。
- 初次测试计数器选错 MySQL 指标，已改为 `information_schema.INNODB_METRICS.lock_deadlocks`；该夹具错误不计为业务 RED。
- GREEN：MySQL 8.0.43，双后端 × `READ COMMITTED` / `REPEATABLE READ` × 14 场景，共 56 项通过。包含原 24 项、20 项普通管理员串行成功、4 项交叉管理员死锁拒绝、8 项实际写入后的死锁/超时回滚。
- Django 现有数据库异常为 HTTP 507，初始测试误设为 500，读取异常处理器后修正断言；没有修改对外 HTTP 映射。
- Django 全量：Ruff 通过，290 passed / 1 skipped。根 unittest：42 tests passed。
- FastAPI `make quality`：Ruff、isort、mypy（151 文件）、迁移检查通过；856 passed / 1 skipped，覆盖率 89.25%。
- 文档 generic profile、API/模型契约、相关 Python 编译及 `git diff --check` 通过；根测试辅助模块 Ruff 通过。
- 清理前核对随机测试库数量为 0；已删除本轮专用容器，未操作其他容器。

## 有限收尾复验（2026-09-08）

- 本次仅更新本任务记录和 `docs/TECH_DEBT.md`。四个业务文件的既有改动复核未发现阻断问题，原回归测试保留；未追加业务改动、测试场景或任务文档。
- Django：在 `backend/` 执行 `.venv/bin/ruff check .` 与 `.venv/bin/pytest -q --tb=short`，290 passed / 1 skipped。
- 根目录：`python3 -m unittest discover -s tests -p 'test_*.py'`，42 tests passed；测试辅助模块 Ruff 与相关 Python 编译通过。
- MySQL：使用两端各自 `.venv/bin/python scripts/verify_mysql_grants.py --backend django|fastapi`，MySQL 8.0.43、两种隔离级别的原有 56 项全部重跑通过。撤权后拒绝提交、正常更新/删除交错、交叉管理员失败及实际写入后的 1213/1205 整体回滚断言均通过；既有批量结果回归验证失败项不计为成功。
- 本次专用容器 `dv-admin-lock-close-20260908-3265f90b` 仅映射 `127.0.0.1:57130`，数据目录为 tmpfs；凭据仅注入测试进程。核对随机测试库剩余 0 后已删除容器，没有连接开发/生产数据库或操作其他容器。
- FastAPI 首次 `make quality` 在迁移阶段被本地旧 `PASSWORD_MIN_LENGTH=8` 配置拒绝；本地配置文件保持不动，复验仅向测试进程注入 `PASSWORD_MIN_LENGTH=15`、`PASSWORD_MAX_LENGTH=128` 和测试专用默认口令，并设置 `UV_CACHE_DIR=/private/tmp/dv-admin-uv-cache`。
- FastAPI 复跑完整 `make quality` 退出码 0：Ruff、isort、mypy（151 文件）、迁移检查通过，856 passed / 1 skipped / 42 warnings，覆盖率 89.25%。
- 文档 generic profile、API/模型契约和完整交付 diff（含两个未跟踪回归脚本及本任务记录）复核通过；未发现阻止本地交付的问题。
- 两端各跳过 1 项需显式开启 `RUN_REAL_BACKEND_PLAYWRIGHT=1` 的前端浏览器用例；前端无改动，按计划未重跑本地前端构建或浏览器。远端 CI、生产环境和负载未验证。
- 终点：当前分支本地可交付，未提交、推送、创建 PR、合并或部署；`.pnpm-store/` 及其他既有内容保留。复杂交错可能明确失败并回滚，仍不承诺任意操作组合无死锁。

## 实验边界

- 专用容器 `dv-admin-lock-order-20260908` 仅暴露本机随机端口 61434，使用临时内存数据目录；不会连接现有开发/生产数据库。
- 正常交错只在真实授权查询之后暂停调度；通过 `data_lock_waits` 观察等待，不能用延时推断成功。
- 故障实验中的 `grant_fault_marker` 是随机测试库里的专用表，仅用于构造实际锁环或锁超时；不属于应用模型或迁移。事务权重用于确定故障注入的受害事务，并用真实错误码和回滚断言校验结果。
- MySQL 死锁默认回滚整个事务，锁等待超时默认只回滚失败语句，因此必须验证应用事务退出后的整体状态。参考 [MySQL 8 错误处理](https://dev.mysql.com/doc/refman/8.0/en/innodb-error-handling.html)。

## Review 与剩余风险

- 复核结论：本轮范围内无阻止交付的问题；仅四个业务文件调整锁顺序，其余为测试及文档。改动留在当前分支，尚未提交或推送。
- 多管理员交叉修改仍可能死锁，本轮验证明确失败和数据一致性，不承诺完全消除死锁；不自动重试非幂等事务。
- 角色删除额外前置一次操作者读取，锁内保护和完整授权复核保持原样；不为此扩大公共服务接口或移除安全检查。
- 复杂授权交错、导入等长事务与负载优化仅在实际出现冲突、锁等待或性能问题时另行评估，不作为本轮完成条件；本轮非生产负载测试，不扩展导入结果体验、异步任务或性能专项。
- 前端没有修改，本轮不重跑前端构建/浏览器；远端 CI 未执行。原 `.pnpm-store/` 保留。
