# Non-secret defaults only. Supply secrets and database/Redis settings at runtime.
DEBUG=False
ENABLE_SWAGGER=False
ALLOWED_HOSTS=127.0.0.1,localhost
EXTRA_INSTALLED_APPS=
CORS_ALLOWED_ORIGINS=http://localhost:9527
