NODE_ENV=production

# just a flag
ENV='staging'

# base api
VITE_APP_BASE_API='/stage-api'

# 项目名称（同时用于页面标题和Logo旁的平台名称）
VITE_APP_TITLE=DV-Admin
# 应用Logo文字（展开时显示，折叠时自动取首字母）
VITE_APP_LOGO_TEXT=DV

# API 版本前缀 (例如: v1, v2)，留空则不使用版本号
VITE_APP_API_VERSION=v1
